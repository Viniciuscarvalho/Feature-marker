Hoje estou com um problema estrutural e de organizacao do feature-marker. O fluxo adequado para ter um melhor proveito seria, Prompt utilizando o modo plan do claude, em seguida ativar a skill no modo interactive para abrir as opções para o usuário, caso ele escolha a primeira que é o fluxo completo com prd, techspec e modelo de tasks, utilizar o plano que foi feito pelo claude como entrada para incrementar o PRD e ser muito mais assertivo, criando um prompt de qualidade com as específicações que o feature-marker já traz, além de ler o arquivo CLAUDE.md para não se perder na execução, isso seria possível? Fazer essa mudança na estrutura da skill e do agent ou teria que fazer muitas modificações?

---

Realize o commit e push, além de criar uma nova relase, /add-to-changelog added 5.2.1 “check if exist plan and go increment PRD”, além de atualizar a versão para instalação via homebrew e também a via NPM
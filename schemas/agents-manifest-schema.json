{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Agent Manifest",
  "description": "Discovered agents available for task routing. Generated by agent-discovery.sh.",
  "type": "object",
  "required": ["agents"],
  "properties": {
    "discovered_at": {
      "type": "string",
      "description": "ISO timestamp of discovery run"
    },
    "project_root": {
      "type": "string",
      "description": "Absolute path to the project root"
    },
    "agents": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "description", "path"],
        "properties": {
          "name": {
            "type": "string",
            "description": "Agent identifier, must be unique"
          },
          "description": {
            "type": "string",
            "description": "What this agent does — used for routing decisions"
          },
          "path": {
            "type": "string",
            "description": "Relative path to the agent file from project root"
          },
          "capabilities": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Keywords this agent handles (auth, testing, review, etc.)"
          },
          "phase": {
            "type": "string",
            "enum": ["implementation", "testing", "review", "any"],
            "description": "Which pipeline phase this agent operates in"
          }
        }
      }
    }
  }
}
